# Go toolchain bump

## Goal

Bump `go.mod` toolchain directive from `go 1.17` to the latest stable Go release
(currently `1.26.x`). 1.17 is several years out of support and triggers
compatibility warnings in modern toolchains.

## Context

User policy (memory: `preference-go-version`): Go projects track the latest
stable Go release. Current system `go` is `go1.26.2`; upstream tag at onboard
time is `go1.26.3`. Old toolchain directive blocks some modern dep versions
(generics, new stdlib APIs).

## File Structure

- Modify: `go.mod` (line 3, `go 1.17` → `go 1.26`)
- Modify: `go.sum` (regenerated by `go mod tidy`)

## Tasks

### Task 1: Bump directive
- [ ] Edit `go.mod` line 3: replace `go 1.17` with `go 1.26`
- [ ] Verify: `head -3 go.mod | tail -1` prints `go 1.26`

### Task 2: Regenerate go.sum
- [ ] `go mod tidy`
- [ ] Verify: `go mod tidy` exits 0 with no further changes when re-run

### Task 3: Build and test still pass
- [ ] `go build ./...` exits 0
- [ ] `go test ./... -count=1 -race` exits 0 (or "no tests" — no regression from baseline)
- [ ] `golangci-lint run ./...` exits 0

### Task 4: Commit
- [ ] `git add go.mod go.sum && git commit -m "go: bump toolchain to 1.26"`

## Verification (end-to-end)

- [ ] Re-run `2026-05-20-onboard-precheck.md` Task 2 — all checks exit 0.
- [ ] `go.mod` line 3 is `go 1.26`.
- [ ] No new lint or vuln findings introduced by the bump.

## Cross-references

- Memory: `preference-go-version` (user prefers latest stable Go).
- Next plan: `2026-05-20-onboard-dep-refresh.md` (deps assume modern toolchain).
